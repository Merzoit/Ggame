# 🚀 Инструкция по деплою GGame

## 📋 Варианты бесплатного хостинга

### Для Django бэкенда:
1. **Railway** (рекомендуется) - https://railway.app
2. **Render** - https://render.com
3. **PythonAnywhere** - https://www.pythonanywhere.com

### Для Vue фронтенда:
1. **Vercel** (рекомендуется) - https://vercel.com
2. **Netlify** - https://netlify.com
3. **GitHub Pages** - для статики

---

## 🎯 Вариант 1: Railway (Django) + Vercel (Vue)

### Шаг 1: Деплой Django на Railway

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `GGame`

3. **Настройте сервис:**
   - Railway автоматически определит Python проект
   - Добавьте PostgreSQL базу данных:
     - Нажмите "+ New" → "Database" → "PostgreSQL"
   - Настройте переменные окружения:
     ```
     SECRET_KEY=ваш-секретный-ключ-здесь
     DEBUG=False
     ALLOWED_HOSTS=ваш-домен.railway.app
     DATABASE_URL=автоматически-из-базы-данных
     CORS_ALLOWED_ORIGINS=https://ваш-фронтенд.vercel.app
     ```

4. **Деплой:**
   - Railway автоматически запустит деплой
   - После деплоя получите URL вашего API (например: `https://ggame-production.up.railway.app`)

### Шаг 2: Деплой Vue на Vercel

1. **Зарегистрируйтесь на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub

2. **Импортируйте проект:**
   - Нажмите "Add New" → "Project"
   - Выберите ваш репозиторий
   - **Важно:** Укажите корневую папку: `frontend-vue`

3. **Настройте сборку:**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

4. **Настройте переменные окружения:**
   ```
   VITE_API_BASE_URL=https://ваш-бэкенд.railway.app/api
   ```

5. **Деплой:**
   - Vercel автоматически задеплоит проект
   - Получите URL фронтенда (например: `https://ggame.vercel.app`)

### Шаг 3: Обновите CORS настройки

В Railway добавьте в переменные окружения:
```
CORS_ALLOWED_ORIGINS=https://ggame.vercel.app
```

---

## 🎯 Вариант 2: Render (Django) + Netlify (Vue)

### Шаг 1: Деплой Django на Render

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте PostgreSQL базу данных:**
   - Dashboard → "New" → "PostgreSQL"
   - Выберите Free план
   - Запомните connection string

3. **Создайте Web Service:**
   - Dashboard → "New" → "Web Service"
   - Подключите GitHub репозиторий
   - Настройки:
     - **Name:** ggame-backend
     - **Environment:** Python 3
     - **Build Command:** `pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate`
     - **Start Command:** `gunicorn ggame.wsgi:application`

4. **Настройте переменные окружения:**
   ```
   SECRET_KEY=сгенерируйте-секретный-ключ
   DEBUG=False
   DATABASE_URL=connection-string-из-базы-данных
   ALLOWED_HOSTS=ggame-backend.onrender.com
   CORS_ALLOWED_ORIGINS=https://ggame-frontend.netlify.app
   ```

5. **Деплой:**
   - Render автоматически задеплоит
   - Получите URL: `https://ggame-backend.onrender.com`

### Шаг 2: Деплой Vue на Netlify

1. **Зарегистрируйтесь на Netlify:**
   - Перейдите на https://netlify.com
   - Войдите через GitHub

2. **Импортируйте проект:**
   - "Add new site" → "Import an existing project"
   - Выберите GitHub репозиторий
   - Настройки:
     - **Base directory:** `frontend-vue`
     - **Build command:** `npm run build`
     - **Publish directory:** `frontend-vue/dist`

3. **Настройте переменные окружения:**
   ```
   VITE_API_BASE_URL=https://ggame-backend.onrender.com/api
   ```

4. **Деплой:**
   - Netlify автоматически задеплоит
   - Получите URL: `https://ggame-frontend.netlify.app`

---

## 🔧 Настройка после деплоя

### 1. Настройте Telegram бота

После деплоя бэкенда настройте вебхук для бота:

```bash
# Установите переменные окружения
export TELEGRAM_WEBHOOK_URL=https://ваш-бэкенд.railway.app
export FRONTEND_URL=https://ваш-фронтенд.vercel.app

# Установите вебхук
python manage.py set_webhook --url https://ваш-бэкенд.railway.app/api/telegram/webhook/
```

### 2. Создайте суперпользователя

Подключитесь к серверу через Railway/Render консоль или SSH:

```bash
python manage.py createsuperuser
```

### 3. Соберите статические файлы

```bash
python manage.py collectstatic --noinput
```

### 4. Примените миграции

```bash
python manage.py migrate
```

### 4. Обновите CORS настройки

В переменных окружения бэкенда укажите URL фронтенда:
```
CORS_ALLOWED_ORIGINS=https://ваш-фронтенд.vercel.app
```

---

## 📝 Переменные окружения

### Django (бэкенд):
```
SECRET_KEY=ваш-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ваш-домен.railway.app
DATABASE_URL=postgresql://...
CORS_ALLOWED_ORIGINS=https://ваш-фронтенд.vercel.app
TELEGRAM_BOT_TOKEN=8567389465:AAGf6VKykyl6REaiDz-Vqu2QTacQbvURS7k
TELEGRAM_WEBHOOK_URL=https://ваш-бэкенд.railway.app
FRONTEND_URL=https://ваш-фронтенд.vercel.app
```

### Vue (фронтенд):
```
VITE_API_BASE_URL=https://ваш-бэкенд.railway.app/api
```

---

## 🎯 Рекомендуемый стек

**Для начала:**
- **Django:** Railway (бесплатный tier)
- **Vue:** Vercel (бесплатный tier)
- **База данных:** PostgreSQL на Railway

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификаты автоматически
- ✅ Простое масштабирование

---

## 🐛 Решение проблем

### CORS ошибки
Убедитесь, что в `CORS_ALLOWED_ORIGINS` указан правильный URL фронтенда.

### Статические файлы не загружаются
Выполните: `python manage.py collectstatic --noinput`

### База данных не подключается
Проверьте `DATABASE_URL` в переменных окружения.

### Миграции не применяются
Добавьте в build command: `python manage.py migrate`

---

## 📚 Полезные ссылки

- Railway Docs: https://docs.railway.app
- Vercel Docs: https://vercel.com/docs
- Render Docs: https://render.com/docs
- Django Deployment: https://docs.djangoproject.com/en/stable/howto/deployment/

---

## ✅ Чеклист деплоя

- [ ] Создан аккаунт на Railway/Render
- [ ] Создан аккаунт на Vercel/Netlify
- [ ] Репозиторий подключен к платформам
- [ ] Настроены переменные окружения
- [ ] Создана PostgreSQL база данных
- [ ] Применены миграции
- [ ] Создан суперпользователь
- [ ] Настроен CORS
- [ ] Фронтенд подключен к бэкенду
- [ ] Протестирована работа API

---

**Удачи с деплоем! 🚀**
