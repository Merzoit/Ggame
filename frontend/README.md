# GGame Frontend

Современный мобильный интерфейс для карточной игры GGame.

## 🚀 Особенности

- **Мобильная оптимизация** - разработан специально для телефонов
- **Современный дизайн** - градиенты, анимации, игровая эстетика
- **Адаптивный интерфейс** - работает на всех размерах экранов
- **Модульная архитектура** - легко расширяемый код
- **Telegram WebApp** - интегрируется с Telegram

## 📁 Структура проекта

```
frontend/
├── index.html          # Главная страница
├── css/
│   ├── styles.css      # Основные стили
│   └── components.css  # Стили компонентов
├── js/
│   ├── config.js       # Конфигурация
│   ├── api.js          # API клиент
│   ├── components.js   # UI компоненты
│   └── app.js          # Основная логика
├── images/             # Изображения
└── README.md           # Документация
```

## 🛠️ Запуск проекта

### Локальный запуск

1. **Запустите Django сервер:**
```bash
cd ../  # Перейти в корневую папку проекта
python manage.py runserver
```

2. **Откройте в браузере:**
```
http://127.0.0.1:8000/frontend/index.html
```

### В Telegram

1. **Создайте Telegram бота** через @BotFather
2. **Настройте WebApp** в настройках бота
3. **Укажите URL:** `https://your-domain.com/frontend/index.html`

## 🎨 Дизайн система

### Цвета
- **Primary:** `#00d4ff` - основной акцент
- **Secondary:** `#ff6b6b` - вторичный акцент
- **Background:** `#0f0f23` - темный фон
- **Cards:** `#1a1a2e` - фон карточек
- **Text:** `#ffffff` / `#b8c5d6` - основной/вторичный текст

### Типографика
- **Шрифт:** Inter (Google Fonts)
- **Размеры:** 14px-32px
- **Веса:** 300, 400, 500, 600, 700

### Компоненты
- **Кнопки:** закругленные, с hover эффектами
- **Карточки:** тени, градиенты, анимации
- **Модалы:** blur backdrop, slide анимации
- **Навигация:** bottom bar для мобильных

## 📱 Мобильная оптимизация

### Viewport
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

### Touch события
- Касания для навигации
- Swipe жесты (можно добавить)
- Tap выделение отключено

### Производительность
- CSS Grid и Flexbox
- Hardware acceleration
- Оптимизированные изображения
- Минимизация reflow/repaint

## 🔧 Разработка

### Добавление новых разделов

1. **Создайте HTML структуру** в `index.html`
2. **Добавьте стили** в `components.css`
3. **Напишите логику** в `app.js`
4. **Настройте навигацию** в `switchSection()`

### Добавление новых компонентов

1. **Создайте функцию** в `components.js`
2. **Используйте паттерн:**
```javascript
createComponentName(data, options = {}) {
    const element = document.createElement('div');
    element.className = 'component-class';
    // ... логика создания
    return element;
}
```

### API интеграция

```javascript
// Получение данных
const data = await api.getDeck();

// Отправка данных
await api.addCardToDeck(cardId, position);

// Обработка ошибок
try {
    await api.someAction();
} catch (error) {
    components.showToast('Ошибка', 'error');
}
```

## 🎮 Функциональность

### ✅ Реализовано
- Главная страница с навигацией
- Раздел колоды (просмотр, редактирование)
- Раздел инвентаря
- Раздел магазина (карты, предметы, валюта)
- База знаний с категориями
- Адаптивный дизайн
- API интеграция
- Компонентная система

### 🚧 В разработке
- Боевой интерфейс
- Анимации карт
- Push уведомления
- Кеширование данных

### 📋 План развития
- [ ] Система боя между игроками
- [ ] Анимации и эффекты
- [ ] Звуковые эффекты
- [ ] Локализация (i18n)
- [ ] PWA возможности
- [ ] Offline режим

## 🐛 Отладка

### Browser DevTools
- **Console:** логи приложения
- **Network:** API запросы
- **Application:** localStorage, Service Workers

### Мобильная отладка
- **Chrome DevTools:** remote debugging
- **Telegram WebApp:** логи в Telegram

### Тестирование
```javascript
// Ручное тестирование
console.log('App state:', APP_STATE);
console.log('Current user:', app.user);

// API тестирование
api.getDeck().then(data => console.log(data));
```

## 📄 Лицензия

MIT License - см. основной проект.
