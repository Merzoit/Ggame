# GGame

🎮 **Telegram Game Bot** - интеллектуальная викторина в Telegram с Django бэкендом.

Проект представляет собой веб-приложение для проведения викторин в Telegram чатах. Пользователи могут создавать игры, присоединяться к ним, отвечать на вопросы и соревноваться за очки.

## Установка и настройка

### Требования
- Python 3.x
- Git
- Windows PowerShell

### Первоначальная настройка проекта

1. **Git уже настроен!** Репозиторий инициализирован, базовые файлы созданы.

2. **Настройка GitHub подключения:**
   - Создайте новый репозиторий на GitHub (https://github.com/new)
   - Назовите его "GGame"
   - Запустите скрипт настройки:
   ```powershell
   .\setup-github.ps1 -RepoUrl "https://github.com/your-username/GGame.git"
   ```

3. **Создайте виртуальное окружение (рекомендуется):**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

5. **Настройте переменные окружения:**
```bash
cp env.example .env
# Отредактируйте .env файл с вашими настройками
```

6. **Примените миграции базы данных:**
```bash
python manage.py migrate
```

7. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```

### Клонирование существующего репозитория
```bash
git clone <repository-url>
cd GGame
```

## Разработка

### Запуск проекта

### Режим разработки:
```bash
python manage.py runserver
```
Сервер будет доступен по адресу: http://127.0.0.1:8000

### Админ панель:
- URL: http://127.0.0.1:8000/admin/
- Логин: admin
- Пароль: admin123

## API Endpoints

### Игры
- `GET /api/games/` - список всех игр
- `POST /api/games/` - создать новую игру
- `POST /api/games/{id}/join/` - присоединиться к игре
- `POST /api/games/{id}/start/` - начать игру

### Вопросы
- `GET /api/questions/` - список вопросов

### Пользователи
- `GET /api/users/` - список пользователей

### Telegram Webhook
- `POST /api/telegram/webhook/` - прием сообщений от Telegram

### Работа с Git

#### Быстрый старт с PowerShell скриптом:

Для удобства создан PowerShell скрипт `git-workflow.ps1`. Запускать из папки проекта:

```powershell
# Проверить статус
.\git-workflow.ps1 -Action status

# Добавить и зафиксировать изменения
.\git-workflow.ps1 -Action add
.\git-workflow.ps1 -Action commit -Message "Описание изменений"

# Отправить/получить изменения
.\git-workflow.ps1 -Action push
.\git-workflow.ps1 -Action pull

# Работа с ветками
.\git-workflow.ps1 -Action branch -Branch "feature/new-feature"
.\git-workflow.ps1 -Action checkout -Branch "main"
.\git-workflow.ps1 -Action merge -Branch "feature/new-feature"

# Показать справку
.\git-workflow.ps1
```

#### Основные команды Git (альтернативный способ):
```bash
# Проверить статус
git status

# Добавить файлы
git add .

# Зафиксировать изменения
git commit -m "Описание изменений"

# Отправить на сервер
git push origin main

# Получить изменения с сервера
git pull origin main
```

#### Рабочий процесс:
1. Создайте новую ветку для каждой фичи: `git checkout -b feature/new-feature`
2. Внесите изменения
3. Зафиксируйте: `git commit -m "Описание"`
4. Перейдите на main: `git checkout main`
5. Обновите main: `git pull origin main`
6. Слейте ветку: `git merge feature/new-feature`
7. Отправьте изменения: `git push origin main`

## Структура проекта

```
GGame/
├── README.md              # Документация проекта
├── .gitignore            # Игнорируемые файлы
├── requirements.txt      # Зависимости Python
├── env.example          # Пример переменных окружения
├── manage.py            # Django management script
├── ggame/               # Основная конфигурация Django
│   ├── __init__.py
│   ├── settings.py      # Настройки Django
│   ├── urls.py          # Основные URL маршруты
│   ├── asgi.py          # ASGI конфигурация
│   └── wsgi.py          # WSGI конфигурация
├── game/                # Приложение игры
│   ├── models.py        # Модели: GameSession, Question, Answer
│   ├── views.py         # API представления
│   ├── serializers.py   # Сериализаторы для API
│   ├── urls.py          # URL маршруты игры
│   └── migrations/      # Миграции базы данных
├── users/               # Приложение пользователей
│   ├── models.py        # Модель TelegramUser
│   └── ...
├── telegram_bot/        # Приложение Telegram бота
│   ├── views.py         # Webhook обработчик
│   └── ...
└── db.sqlite3          # База данных (разработка)
```

## 🤖 Telegram бот

Проект включает Telegram бота @MerzoitCodeBot для регистрации пользователей и доступа к игре.

### Настройка бота
1. **Токен бота:** `8567389465:AAGf6VKykyl6REaiDz-Vqu2QTacQbvURS7k`
2. **Проверка:** `python manage.py bot_info`
3. **Тестирование:** `python manage.py test_bot --chat-id YOUR_CHAT_ID`

### Функциональность
- Команда `/start` регистрирует пользователя
- Кнопка "🎮 Начать игру" открывает веб-приложение
- Интеграция с пользовательской системой Django

**Подробная инструкция:** См. [TELEGRAM_BOT_SETUP.md](TELEGRAM_BOT_SETUP.md)

## 🚀 Деплой на хостинг

### Быстрый деплой (рекомендуется)

**Django бэкенд на Railway:**
1. Зарегистрируйтесь на https://railway.app
2. Подключите GitHub репозиторий
3. Добавьте PostgreSQL базу данных
4. Настройте переменные окружения (см. DEPLOY.md)

**Vue фронтенд на Vercel:**
1. Зарегистрируйтесь на https://vercel.com
2. Импортируйте проект (папка `frontend-vue`)
3. Настройте `VITE_API_BASE_URL` на URL вашего бэкенда

**Подробная инструкция:** См. [DEPLOY.md](DEPLOY.md)

### Альтернативные варианты

- **Render** + **Netlify** - также бесплатные варианты
- **PythonAnywhere** - для Django
- **GitHub Pages** - для статического фронтенда

Все файлы для деплоя уже подготовлены в проекте!

## 🤖 Telegram Bot

Проект включает Telegram бота для регистрации пользователей и запуска игры.

### Настройка бота:

1. **Бот уже настроен** с токеном `8567389465:AAGf6VKykyl6REaiDz-Vqu2QTacQbvURS7k`
2. **Имя бота:** @MerzoitCodeBot
3. **Функционал:**
   - Регистрация пользователей по команде `/start`
   - Создание профиля в базе данных
   - Кнопка "Начать игру" для открытия веб-приложения

### Тестирование бота:

```bash
# Проверить информацию о боте
python manage.py bot_info

# Для продакшена установить вебхук
python manage.py set_webhook --url https://your-domain.com/api/telegram/webhook/
```

### Локальное тестирование:

Для локального тестирования используйте ngrok:
```bash
# Установите ngrok
# Запустите: ngrok http 8000
# Скопируйте HTTPS URL и используйте для вебхука
python manage.py set_webhook --url https://your-ngrok-url.ngrok.io/api/telegram/webhook/
```

## Разработка

### Следующие шаги:

1. **Настройка Telegram Bot:**
   - Создать бота через @BotFather
   - Настроить webhook URL
   - Реализовать обработку команд

2. **Игровая логика:**
   - Создать систему вопросов и ответов
   - Реализовать таймеры и очки
   - Добавить разные типы игр

3. **Интерфейс:**
   - Создать Telegram клавиатуры
   - Добавить inline кнопки
   - Реализовать меню навигации

4. **Дополнительные фичи:**
   - Статистика игроков
   - Рейтинговые таблицы
   - Турниры и чемпионаты

### Команды для разработки:

```bash
# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Запуск тестов
python manage.py test

# Проверка кода
python manage.py check
```

## Авторы

- [Merzoit] - Основной разработчик

## Лицензия

Этот проект лицензирован под MIT License.
