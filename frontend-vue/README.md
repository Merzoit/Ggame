# GGame Frontend - Vue.js

Современный мобильный интерфейс для карточной игры GGame на Vue 3 + Vite.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📁 Структура проекта

```
frontend-vue/
├── src/
│   ├── assets/          # Статические ресурсы
│   │   └── styles/      # Глобальные стили
│   ├── components/      # Vue компоненты
│   │   ├── cards/       # Компоненты карт
│   │   ├── common/      # Общие компоненты
│   │   ├── layout/      # Компоненты макета
│   │   └── modals/      # Модальные окна
│   ├── router/          # Vue Router
│   ├── services/        # API сервисы
│   ├── stores/          # Pinia stores
│   ├── views/           # Страницы приложения
│   ├── App.vue          # Главный компонент
│   └── main.js          # Точка входа
├── public/              # Публичные файлы
├── index.html           # HTML шаблон
└── vite.config.js      # Конфигурация Vite
```

## 🎨 Особенности

- **Vue 3 Composition API** - современный подход к разработке
- **Vue Router** - навигация между страницами
- **Pinia** - управление состоянием
- **Axios** - HTTP клиент для API
- **Современный дизайн** - градиенты, анимации, темная тема
- **Мобильная оптимизация** - адаптивный дизайн для телефонов
- **Hot Module Replacement** - мгновенное обновление при разработке

## 🔧 Конфигурация

### API URL

По умолчанию API запросы идут на `http://127.0.0.1:8000/api`

Для изменения создайте файл `.env`:
```
VITE_API_BASE_URL=http://your-api-url.com/api
```

### Проксирование

В режиме разработки Vite автоматически проксирует запросы к `/api` на Django сервер.

## 📱 Страницы

- **/** - Главная (Колода)
- **/inventory** - Инвентарь
- **/shop** - Магазин
- **/knowledge** - База знаний

## 🎮 Компоненты

### CardComponent
Компонент для отображения карты с характеристиками.

### AppHeader
Шапка приложения с информацией о пользователе и валютой.

### AppNavigation
Нижняя навигационная панель для мобильных устройств.

### ToastContainer
Система уведомлений для пользователя.

## 🔌 API Интеграция

Все API запросы идут через `src/services/api.js`:

```javascript
import api from '@/services/api'

// Получить колоду
const deck = await api.getDeck()

// Добавить карту в колоду
await api.addCardToDeck(cardId, position)

// Купить карту
await api.acquireCard(templateId)
```

## 📦 Store (Pinia)

Состояние приложения управляется через Pinia store:

```javascript
import { useGameStore } from '@/stores/game'

const gameStore = useGameStore()

// Загрузить данные
await gameStore.fetchDeck()
await gameStore.fetchInventory()

// Использовать данные
const deck = gameStore.deck
const coins = gameStore.userCoins
```

## 🎨 Стилизация

Стили используют CSS переменные для темизации:

```css
:root {
  --bg-primary: #0a0a0f;
  --accent-primary: #6366f1;
  --text-primary: #ffffff;
}
```

## 🚀 Деплой

### Статический хостинг

1. Соберите проект: `npm run build`
2. Загрузите папку `dist/` на ваш хостинг
3. Настройте API URL в `.env.production`

### Telegram WebApp

1. Соберите проект для продакшена
2. Загрузите на сервер
3. Укажите URL в настройках Telegram бота

## 🐛 Отладка

### Vue DevTools
Установите расширение Vue DevTools для браузера для отладки компонентов и состояния.

### Console
Все ошибки логируются в консоль браузера.

## 📝 TODO

- [ ] Система боя
- [ ] Анимации карт
- [ ] Push уведомления
- [ ] PWA поддержка
- [ ] Оптимизация производительности

## 📄 Лицензия

MIT License